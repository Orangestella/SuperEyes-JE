package util;

public class GameConfigLoader {
    private final ConfigValidator configValidator;

    public GameConfigLoader(ConfigValidator configValidator) {
        this.configValidator = configValidator;
    }

}
